spring:
  datasource:
    url: jdbc:postgresql://db:5432/romashka2
    username: postgres
    password: 79182872449

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
